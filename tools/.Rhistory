# ============================================================
# Extra outputs for the case study: pre-period MSPE + spec-ladder
# sensitivity. Run AFTER the emitter, so sc_black/sc_white/etc. exist.
# ============================================================
library(dplyr)
# ---- 1. Pre-period MSPE: Texas vs the placebo distribution ----
# grab_significance() returns one row per unit with pre_mspe, post_mspe,
# and mspe_ratio. Texas is type "Treated"; donors are "Donor".
sig_b <- grab_significance(sc_black)
