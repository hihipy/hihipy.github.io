# >>> managed gitignore: begin (gitignore-forge.py) >>>
# Generated 2026-08-04 22:03 for hihipy.github.io.
# Detected stacks: python, r, node.
# Edits inside this block are overwritten. Put repo-specific rules below it.

# --- macOS -------------------------------------------------------------------
.DS_Store
._*
.AppleDouble
.AppleDB
.AppleDesktop
.LSOverride
# The next line is "Icon" followed by two carriage returns. Git strips one, and
# the macOS custom-icon file carries the other. A plain "Icon" line would hide
# every path with that name. Verify with: grep -c $'Icon\r\r' .gitignore
Icon
.Spotlight-V100
.DocumentRevisions-V100
.TemporaryItems
.Trashes
.fseventsd
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.apdisk
Network Trash Folder
Temporary Items
# --- Windows and Linux artifacts ---------------------------------------------
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/
*.stackdump
*.lnk
.directory
.Trash-*
.nfs*
# --- cloud sync --------------------------------------------------------------
*.icloud
.dropbox
.dropbox.attr
.dropbox.cache
# --- editors and IDEs --------------------------------------------------------
# .vscode/* rather than .vscode/, so the negations below stay reachable. A
# negation cannot re-include a file whose parent directory is excluded.
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
*.code-workspace
.idea/
*.iml
*.ipr
*.iws
.fleet/
.zed/
.history/
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*.swn
*~
Session.vim
.netrwhist
.ionide/
# --- agent and assistant local state -----------------------------------------
.claude/settings.local.json
# --- secrets and credentials -------------------------------------------------
# Deliberately broad. A leaked key costs more than a hidden fixture. Where a
# repo needs a fixture certificate, add a scoped negation below the block, for
# example !tests/fixtures/*.pem, which works because tests/fixtures/ is not
# itself ignored.
.env
.env.*
!.env.example
!.env.sample
!.env.template
*.pem
*.p12
*.pfx
*.keystore
*.jks
id_rsa
id_dsa
id_ecdsa
id_ed25519
*.ppk
# "*.key" is deliberately absent: Keynote decks are directories named *.key.
secrets.json
credentials.json
service-account*.json
.secrets/
# --- local databases and dumps -----------------------------------------------
*.sqlite
*.sqlite3
*.db-journal
*.db-shm
*.db-wal
*.dump
# --- build noise and scratch -------------------------------------------------
*.log
*.tmp
*.temp
*.bak
*.orig
*.rej
*.pid
.tmp/
.scratch/
# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
# *.so is unanchored on purpose: compiled extensions land beside their sources.
*.so
.Python
/build/
/dist/
/develop-eggs/
/eggs/
.eggs/
/sdist/
/wheels/
share/python-wheels/
*.egg-info/
*.egg
.installed.cfg
/MANIFEST
pip-log.txt
pip-delete-this-directory.txt
.tox/
.nox/
.coverage
.coverage.*
coverage.xml
nosetests.xml
*.cover
/htmlcov/
.pytest_cache/
.hypothesis/
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
.ruff_cache/
.pdm-python
.pdm-build/
__pypackages__/
venv/
.venv/
/env/
/ENV/
.ipynb_checkpoints/
/profile_default/
# --- R -----------------------------------------------------------------------
.Rproj.user/
.Rhistory
.Rapp.history
.RData
.RDataTmp
.Ruserdata
.Renviron
.Rprofile.local
.httr-oauth
# knitr writes <docname>_cache/ beside its source, so this cannot be anchored.
# It is the reason this section is gated rather than universal.
*_cache/
*.utf8.md
*.knit.md
*.Rcheck/
renv/library/
renv/local/
renv/staging/
renv/python/
renv/sandbox/
# --- Node and web ------------------------------------------------------------
node_modules/
jspm_packages/
.npm
# Delete the next two lines in a Yarn zero-installs repo, where .yarn/cache is
# committed on purpose.
.yarn/cache/
.yarn/install-state.gz
.pnpm-store/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.eslintcache
.stylelintcache
.parcel-cache/
.cache/
.next/
.nuxt/
.svelte-kit/
.turbo/
.vercel/
.netlify/
/coverage/
*.tsbuildinfo
# <<< managed gitignore: end <<<

# --- local rules: edit freely, never overwritten -----------------------------
# Hugo build output
/public/
/resources/_gen/
.hugo_build.lock

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Node modules
node_modules/
package-lock.json

# generated + local backups
public/
resources/
*.backup-*
.DS_Store
tools/texas_emit_charts.R

# Penobscot real-data backups (never publish)
static/data/*.bak
!static/data/*.sqlite
!static/data/*.sqlite
